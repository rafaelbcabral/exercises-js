// Crie uma função que receba um array e retorne
// todos os elementos desse array com o valor triplicado.

const triplo = array => array.map( e => e * 3 );

console.log( triplo( [ 1, 2, 3, 4, 5, 6 ] ) );
